name: Monaco Angular PR (Skip)

on:
  pull_request:
    branches:
      - master
    paths-ignore:
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "pkg/**/*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No relevant files changed, skipping."